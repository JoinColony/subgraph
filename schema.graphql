type Colony @entity {
  id: ID! # Address
  ensName: String
  orbitAddress: String
  index: BigInt!
  token: String # Address
  domains: [Domain!]!
}

type Domain @entity {
  id: ID! # colonyAddress_domainId
  index: BigInt!
  parent: Domain
  name: String
}