name: Triggered Tasks

on:
  repository_dispatch:

jobs:
  deploy-goerli:
    name: Build and push subgraph
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.client_payload.SUBGRAPH_COMMIT }}
      - name: Install needed dependencies
        run: npm i
      - name: Build the graph
        run: npm run build-goerli
        env:
          CONTRACT_COMMIT: ${{ github.event.client_payload.CONTRACT_COMMIT}}
      - name: Deploy the graph
        run: npm run deploy-goerli
        env:
          GRAPH_TOKEN: ${{ secrets.GRAPH_TOKEN }}
